# CPU-only local setup (macOS / Linux without CUDA).
#
# 1) Create/activate venv, then install PyTorch CPU wheels:
#    pip install torch --index-url https://download.pytorch.org/whl/cpu
# 2) Then:
#    pip install -r requirements-cpu.txt
#
# (triton is omitted — CUDA-oriented and often unavailable on macOS; not imported by this repo.)

# Fused `adam-atan2` has no CPU/macOS extension on PyPI; pure PyTorch port is API-compatible for training.
adam-atan2-pytorch
einops
tqdm
coolname
pydantic
argdantic
wandb
omegaconf
hydra-core
huggingface_hub
packaging
ninja
wheel
setuptools
setuptools-scm
pydantic-core
# Pin for prebuilt wheels on macOS (avoid llvmlite source builds).
llvmlite==0.45.1
numba==0.62.1
# PyTorch 2.2.x wheels expect NumPy 1.x ABI. Pin for a full wheel (avoid a half-removed/broken tree).
numpy==1.26.4
pyyaml
